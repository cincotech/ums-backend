@startuml
start
:Teacher enters grades;
:Validate grade data;
if (Valid?) then (yes)
  :Save grades;
  :Calculate statistics;
  :Notify students;
else (no)
  :Show validation errors;
endif
stop
@enduml
